{% extends 'base.html.twig' %}

{% block title %}Perfect Registration - Home{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
      <div class="col-sm">  
        {% if app.user %}
      <p class="h1 arvos">Bonjour {{ app.user.firstname }}.<br></p>
       
         {% else %}
        <p class="h1 text-dark arvos">
            Le meilleur formulaire d'inscription qui existe.</p>
    
        {% endif %}
      </div>
   
    </div>
</div>


{% endblock %}
